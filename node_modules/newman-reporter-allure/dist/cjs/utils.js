"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractMeta = void 0;
var _sdk = require("allure-js-commons/sdk");
var extractMeta = eventList => {
  var labels = [];
  eventList.each(event => {
    if (event.listen === "test" && event.script.exec) {
      event.script.exec.forEach(line => {
        var isCommentLine = line.trim().startsWith("//");
        if (!isCommentLine) {
          return;
        }
        var trimmedCommentValue = line.trim().replace("//", "").trim();
        var metadata = (0, _sdk.extractMetadataFromString)(trimmedCommentValue);
        labels.push(...metadata.labels);
      });
    }
  });
  return {
    labels
  };
};
exports.extractMeta = extractMeta;
//# sourceMappingURL=utils.js.map